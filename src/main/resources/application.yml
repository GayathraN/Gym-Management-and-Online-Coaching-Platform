server:
  port: 8011

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gymDB?createDatabaseIfNotExist=true
    username: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

